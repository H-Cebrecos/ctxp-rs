[package]
name = "ctxp"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
rand = "0.8"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "encode_text"
harness = false
